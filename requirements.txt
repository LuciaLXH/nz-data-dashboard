# Python deps for the data pipeline (W1+W2).
# IO/orchestration only — business logic lives in sql/ (DuckDB).
requests==2.32.3
openpyxl==3.1.5
pandas==2.2.3
jsonschema==4.23.0
duckdb==1.5.5
pytest==7.4.4
